This Project is Part 1 of a simple messenger app, built using Maven and Java! 

!!!
To run project in the console, use "mvm clean compile", and then "mvn exec:java" to build ( Specified as a build plugin in pom.xml ). 
